# add the examples subdirectories
add_subdirectory(XOR)
add_subdirectory(NumClassifier)
add_subdirectory(IrisClassification)
add_subdirectory(Maze)
if(EvoAI_SFML_AVAILABLE)
    add_subdirectory(CellSim)
else()
    message(STATUS "Skipping CellSim - requires SFML")
endif()
